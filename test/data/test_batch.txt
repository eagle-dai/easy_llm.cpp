Give a concise summary (<=80 words) of how byte-pair encoding works; end with a 3-item comma-separated list of risks.
把下面句子改写为更正式的中文，不改变事实：这个模型挺快的，但有时会卡。
给出一个 5 行以内的 Python 函数 is_prime(n)，只返回 True/False，不要解释。
用英文列出 4 个测试原则，每条不超过 6 个词，用分号分隔为一行。
Given list [3,1,4,1,5,9], output the list sorted descending as JSON array only.
简要解释什么是注意力机制，并在末尾附上一个10字以内的英文tagline，格式：Tagline: <...>
Convert 37 Celsius to Fahrenheit, output as F=xx.x with one decimal.
Translate to English: 模型在小批量上表现稳定。
Given the sequence 2, 4, 7, 11, 16, ?, output the next number only.
Return a CSV header for fields id,query,response,time_ms and one example row with id=1, query=hello, response=world, time_ms=5. Put header and row on one line separated by |.
Write a short checklist (exactly 4 items) for debugging a crash; format as '1) ...; 2) ...; 3) ...; 4) ...' on one line.
提供一个单行 YAML（flow style），键为 gpu, batch, precision，值为 A100, 20, bf16。
Answer in one line: If a model outputs 512 tokens at 128 tokens/s, how many seconds? Use seconds=<value>.
